import Layout from "@/components/common/Layout";

const Home = () => {
    return (
        <Layout>
            <section className="h-screen">Home</section>
        </Layout>
    );
};

export default Home;
