import Layout from "@/components/common/Layout";

const NotFound = () => {
    return (
        <Layout>
            <section>Not found</section>
        </Layout>
    );
};

export default NotFound;
